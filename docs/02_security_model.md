# 安全模型

## 信任模型

1. 手机和电脑都是用户自己的设备
2. 已配对手机被视为可信控制端
3. 不能让陌生手机加入
4. 不能让 Relay 伪造手机命令
5. 不能只靠 token 授权

## 签名机制

- 算法：Ed25519
- 签名内容：signing string (v1 + 字段拼接)
- 禁止直接签 JSON.stringify
- 验签流程包含 13 步检查

## 威胁模型

- Relay 可能被入侵 → 不影响安全性（只转发）
- 中间人攻击 → HTTPS/WSS 保护
- 重放攻击 → nonce + timestamp 检查
