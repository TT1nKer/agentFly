# 系统架构

## 三层结构

1. **手机 App** (Flutter) - 控制端，生成密钥对，签名所有控制消息
2. **Relay** (Rust/Axum) - 转发层，不可信，只负责转发消息
3. **Bridge** (Rust) - 执行端，验签，管理 tmux session

## 安全模型

- 手机私钥永远不离开手机
- Bridge 保存手机公钥
- 所有控制消息必须签名
- Relay 不可信，不执行任何命令
